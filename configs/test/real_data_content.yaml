# experiment name
EXP_NAME: Old Loss Function
# random number seed
SEED: 0
# Super resolution magnification factor
SCALE: 4
# run device model
DEVICE_ID: 0
# Model weights path
MODEL_WEIGHTS_PATH: "/mnt/c/users/jfegg/documents/cai_lab/SRGAN-PyTorch/samples/Real_Data_Old_Loss1/g_epoch_30.pth.tar"

# Model define
MODEL:
  G:
    NAME: srresnet_x4
    IN_CHANNELS: 1
    OUT_CHANNELS: 1
    CHANNELS: 64
    NUM_RCB: 16
    COMPILED: False

TEST:
  DATASET:
    PAIRED_TEST_GT_IMAGES_DIR: "./data/real_hr_test"
    PAIRED_TEST_LR_IMAGES_DIR: "./data/real_lr_test"

  # Test hyperparameters
  HYP:
    IMGS_PER_BATCH: 1
    SHUFFLE: False
    NUM_WORKERS: 1
    PIN_MEMORY: True
    PERSISTENT_WORKERS: True

  # Only test Y channel
  ONLY_TEST_Y_CHANNEL: True

  SAVE_IMAGE_DIR: "./results/real_data"